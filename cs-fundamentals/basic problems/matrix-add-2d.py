# Given two matrices, create a new matrix with matrix1+matrix2
'''
Matrix addition requirements:
	Two matrix can be add or subtracted iff they have the same dimension i.e same row and col

'''

matrix1 = [[1,-2, 2], [4,-5,6], [7,-8, -2]]
matrix2 = [[2,0,-3], [6,-1,6], [4,-4,9]]
result_matx = []

# print len(matrix1) # this will print the total rows in a matrix

row = len(matrix1)
	pass
	print rows[]